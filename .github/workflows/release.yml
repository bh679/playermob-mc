name: Release

# Releases are dispatch-only. There is intentionally no `push: tags` trigger:
# the release workflow is the SOURCE of tags, not a consumer. A tag pushed by
# any other path (manual `git tag && push`, `gh release create`, stray script)
# becomes a visible orphan with no release attached.

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to create + release (e.g. v0.2.0). Will be created from main HEAD — must NOT already exist."
        required: true
        type: string
      notify_discord:
        description: "Force a Discord announcement. Otherwise Discord only fires on a MAJOR bump (X.0.0)."
        required: false
        type: boolean
        default: false
      auto:
        description: "Set true when dispatched by an automated cascade. Manual dispatches leave this false; the changelog mark-released step only runs when auto is false."
        required: false
        type: boolean
        default: false
      republish:
        description: "Recovery for a partial-publish failure: re-run for a tag/release that ALREADY exists. Skips the tag-exists guard; the GitHub release is updated in place and mc-publish (fail-mode:warn) no-ops already-published platform versions while publishing the missing combo(s)."
        required: false
        type: boolean
        default: false
      changelog:
        description: "Release notes (Markdown) for the GitHub release body + Modrinth/CurseForge changelog. Blank = auto-generated notes only."
        required: false
        type: string
        default: ""

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      RELEASE_TAG: ${{ inputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v3

      - name: Verify input tag matches mod_version on main
        run: |
          TAG="${RELEASE_TAG#v}"
          GRADLE_VERSION=$(grep '^mod_version=' gradle.properties | cut -d= -f2)
          if [ "$TAG" != "$GRADLE_VERSION" ]; then
            echo "::error::Input tag $RELEASE_TAG does not match mod_version=$GRADLE_VERSION on main. Bump gradle.properties to $TAG (or re-dispatch with tag=v$GRADLE_VERSION) before releasing."
            exit 1
          fi
          echo "MOD_VERSION=$GRADLE_VERSION" >> "$GITHUB_ENV"

      # Skipped on republish: recovery dispatches intentionally target an
      # existing tag/release (action-gh-release updates it in place below).
      - name: Check tag does not already exist
        if: ${{ inputs.republish == false }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh api "repos/${GITHUB_REPOSITORY}/git/refs/tags/${RELEASE_TAG}" >/dev/null 2>&1; then
            echo "::error::Tag $RELEASE_TAG already exists on remote (likely an orphan from before the dispatch-only flow). Delete it first: gh api -X DELETE repos/${GITHUB_REPOSITORY}/git/refs/tags/${RELEASE_TAG} — or re-dispatch with republish=true to update the existing release."
            exit 1
          fi

      - name: Compute release type from tag
        id: release_type
        run: |
          TAG_NUMERIC="${RELEASE_TAG#v}"
          MAJOR="${TAG_NUMERIC%%.*}"
          REST="${TAG_NUMERIC#*.}"
          MINOR="${REST%%.*}"
          PATCH="${REST#*.}"
          if [ "$MAJOR" -gt 0 ] 2>/dev/null; then
            echo "type=release" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
            echo "Release type: release (tag $RELEASE_TAG, MAJOR=$MAJOR > 0)"
          else
            echo "type=beta" >> "$GITHUB_OUTPUT"
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
            echo "Release type: beta (tag $RELEASE_TAG, MAJOR=$MAJOR — pre-1.0)"
          fi
          if [ "$MAJOR" -gt 0 ] 2>/dev/null && [ "$MINOR" = "0" ] && [ "$PATCH" = "0" ]; then
            echo "major_bump=true" >> "$GITHUB_OUTPUT"
            echo "Major bump detected ($RELEASE_TAG) — Discord will fire."
          else
            echo "major_bump=false" >> "$GITHUB_OUTPUT"
            echo "Not a major bump ($RELEASE_TAG) — Discord only fires if notify_discord=true."
          fi

      - run: ./gradlew chiseledBuild --no-daemon

      # Stonecutter's chiseledBuild collects every (loader, MC version) production
      # + sources jar under build/libs/<mod_version>/<loader>/. Stage the production
      # jars (jar names carry a +<mc> build-metadata suffix, e.g. ...+1.21.1.jar) into
      # one dir so the GitHub-release upload has a single glob target; *-sources.jar
      # are filtered out here rather than in every upload step.
      - name: Stage loader jars for release
        run: |
          mkdir -p release-artifacts
          find "build/libs/${MOD_VERSION}" -name 'playermob-*.jar' ! -name '*-sources.jar' \
            -exec cp {} release-artifacts/ \;
          echo "=== release-artifacts contents ==="
          ls -la release-artifacts/
          # Sanity check: exactly 7 jars — Fabric+Forge @1.20.1, Fabric+Forge+NeoForge @1.21.1,
          # Fabric+NeoForge @26.2 (Forge dropped on 26.x).
          COUNT=$(ls release-artifacts/*.jar | wc -l)
          if [ "$COUNT" -ne 7 ]; then
            echo "::error::Expected 7 loader/version jars in release-artifacts/, found $COUNT"
            exit 1
          fi

      - name: Generate release notes
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          TAG: ${{ env.RELEASE_TAG }}
          CHANGELOG_INPUT: ${{ inputs.changelog }}
        run: |
          if [ -n "$CHANGELOG_INPUT" ]; then
            printf '%s\n' "$CHANGELOG_INPUT" > release-notes.md
            echo "Using operator-provided changelog input ($(wc -c < release-notes.md) bytes)."
          else
            gh api -X POST \
              -H "Accept: application/vnd.github+json" \
              "repos/$REPO/releases/generate-notes" \
              -f tag_name="$TAG" \
              -f target_commitish=main \
              --jq .body > release-notes.md
            if [ ! -s release-notes.md ]; then
              echo "Initial release — no prior tag to diff against." > release-notes.md
            fi
          fi
          echo "::group::Generated release notes"
          cat release-notes.md
          echo "::endgroup::"

      # action-gh-release is idempotent: on a republish dispatch it updates the
      # existing release/tag in place rather than failing.
      - name: Create GitHub Release
        id: gh_release
        uses: softprops/action-gh-release@v2
        with:
          name: PlayerMob ${{ env.RELEASE_TAG }}
          tag_name: ${{ env.RELEASE_TAG }}
          body_path: release-notes.md
          files: release-artifacts/*.jar
          fail_on_unmatched_files: true
          prerelease: ${{ steps.release_type.outputs.prerelease == 'true' }}

      - name: Check mod-platform publish prerequisites
        id: publish_check
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
          CURSEFORGE_TOKEN: ${{ secrets.CURSEFORGE_TOKEN }}
          MODRINTH_PROJECT_ID: ${{ vars.MODRINTH_PROJECT_ID }}
          CURSEFORGE_PROJECT_ID: ${{ vars.CURSEFORGE_PROJECT_ID }}
        run: |
          MODRINTH_READY=false
          CURSEFORGE_READY=false
          if [ -n "$MODRINTH_TOKEN" ] && [ -n "$MODRINTH_PROJECT_ID" ]; then
            MODRINTH_READY=true
          else
            echo "::warning::Modrinth secret/variable missing — skipping Modrinth publish."
          fi
          if [ -n "$CURSEFORGE_TOKEN" ] && [ -n "$CURSEFORGE_PROJECT_ID" ]; then
            CURSEFORGE_READY=true
          else
            echo "::warning::CurseForge secret/variable missing — skipping CurseForge publish."
          fi
          echo "modrinth=$MODRINTH_READY" >> "$GITHUB_OUTPUT"
          echo "curseforge=$CURSEFORGE_READY" >> "$GITHUB_OUTPUT"

      # Each (loader, MC version) publishes as its own Modrinth/CurseForge version —
      # exactly one loader + one game version + one jar — so the launchers and website
      # filters auto-pick the right file for the user's installed loader AND MC version.
      # version_number uses a SemVer build-metadata suffix (e.g. v0.48.0+fabric-1.21.1)
      # so all five versions stay distinct while sharing the human-readable release tag.
      # NeoForge ships 1.21.1 only (no standalone 1.20.1 artifact). The 1.21.1 step ids
      # stay publish_{fabric,forge,neoforge} so the Discord summary keeps resolving them.
      - name: Publish Fabric 1.21.1 to Modrinth + CurseForge
        id: publish_fabric
        if: steps.publish_check.outputs.modrinth == 'true' || steps.publish_check.outputs.curseforge == 'true'
        uses: ./.github/actions/publish-with-retry
        with:
          modrinth-id: ${{ steps.publish_check.outputs.modrinth == 'true' && vars.MODRINTH_PROJECT_ID || '' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: ${{ steps.publish_check.outputs.curseforge == 'true' && vars.CURSEFORGE_PROJECT_ID || '' }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          files: release-artifacts/playermob-fabric-${{ env.MOD_VERSION }}+1.21.1.jar
          name: PlayerMob ${{ env.RELEASE_TAG }} (Fabric 1.21.1)
          version: ${{ env.RELEASE_TAG }}+fabric-1.21.1
          version-type: ${{ steps.release_type.outputs.type }}
          changelog-file: release-notes.md
          loaders: fabric
          game-versions: 1.21.1
          java: 21

      - name: Publish Fabric 1.20.1 to Modrinth + CurseForge
        id: publish_fabric_1201
        if: steps.publish_check.outputs.modrinth == 'true' || steps.publish_check.outputs.curseforge == 'true'
        uses: ./.github/actions/publish-with-retry
        with:
          modrinth-id: ${{ steps.publish_check.outputs.modrinth == 'true' && vars.MODRINTH_PROJECT_ID || '' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: ${{ steps.publish_check.outputs.curseforge == 'true' && vars.CURSEFORGE_PROJECT_ID || '' }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          files: release-artifacts/playermob-fabric-${{ env.MOD_VERSION }}+1.20.1.jar
          name: PlayerMob ${{ env.RELEASE_TAG }} (Fabric 1.20.1)
          version: ${{ env.RELEASE_TAG }}+fabric-1.20.1
          version-type: ${{ steps.release_type.outputs.type }}
          changelog-file: release-notes.md
          loaders: fabric
          game-versions: 1.20.1
          java: 17

      - name: Publish Forge 1.21.1 to Modrinth + CurseForge
        id: publish_forge
        if: steps.publish_check.outputs.modrinth == 'true' || steps.publish_check.outputs.curseforge == 'true'
        uses: ./.github/actions/publish-with-retry
        with:
          modrinth-id: ${{ steps.publish_check.outputs.modrinth == 'true' && vars.MODRINTH_PROJECT_ID || '' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: ${{ steps.publish_check.outputs.curseforge == 'true' && vars.CURSEFORGE_PROJECT_ID || '' }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          files: release-artifacts/playermob-forge-${{ env.MOD_VERSION }}+1.21.1.jar
          name: PlayerMob ${{ env.RELEASE_TAG }} (Forge 1.21.1)
          version: ${{ env.RELEASE_TAG }}+forge-1.21.1
          version-type: ${{ steps.release_type.outputs.type }}
          changelog-file: release-notes.md
          loaders: forge
          game-versions: 1.21.1
          java: 21

      - name: Publish Forge 1.20.1 to Modrinth + CurseForge
        id: publish_forge_1201
        if: steps.publish_check.outputs.modrinth == 'true' || steps.publish_check.outputs.curseforge == 'true'
        uses: ./.github/actions/publish-with-retry
        with:
          modrinth-id: ${{ steps.publish_check.outputs.modrinth == 'true' && vars.MODRINTH_PROJECT_ID || '' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: ${{ steps.publish_check.outputs.curseforge == 'true' && vars.CURSEFORGE_PROJECT_ID || '' }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          files: release-artifacts/playermob-forge-${{ env.MOD_VERSION }}+1.20.1.jar
          name: PlayerMob ${{ env.RELEASE_TAG }} (Forge 1.20.1)
          version: ${{ env.RELEASE_TAG }}+forge-1.20.1
          version-type: ${{ steps.release_type.outputs.type }}
          changelog-file: release-notes.md
          loaders: forge
          game-versions: 1.20.1
          java: 17

      - name: Publish NeoForge 1.21.1 to Modrinth + CurseForge
        id: publish_neoforge
        if: steps.publish_check.outputs.modrinth == 'true' || steps.publish_check.outputs.curseforge == 'true'
        uses: ./.github/actions/publish-with-retry
        with:
          modrinth-id: ${{ steps.publish_check.outputs.modrinth == 'true' && vars.MODRINTH_PROJECT_ID || '' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: ${{ steps.publish_check.outputs.curseforge == 'true' && vars.CURSEFORGE_PROJECT_ID || '' }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          files: release-artifacts/playermob-neoforge-${{ env.MOD_VERSION }}+1.21.1.jar
          name: PlayerMob ${{ env.RELEASE_TAG }} (NeoForge 1.21.1)
          version: ${{ env.RELEASE_TAG }}+neoforge-1.21.1
          version-type: ${{ steps.release_type.outputs.type }}
          changelog-file: release-notes.md
          loaders: neoforge
          game-versions: 1.21.1
          java: 21
          # Dungeon Train is an optional NeoForge-only integration: PlayerMob detects it at
          # runtime (ModList.isLoaded("dungeontrain")) and confines mobs to the train. The
          # PUBLISH SLUG is "dungeon-train" on both platforms (curseforge id 1527512); the
          # runtime mod id "dungeontrain" is unrelated to the slug. Declared only on the
          # NeoForge file since DT cannot load on Fabric/Forge.
          dependencies: |
            dungeon-train(optional){curseforge:dungeon-train}{modrinth:dungeon-train}

      - name: Publish Fabric 26.2 to Modrinth + CurseForge
        id: publish_fabric_262
        if: steps.publish_check.outputs.modrinth == 'true' || steps.publish_check.outputs.curseforge == 'true'
        uses: ./.github/actions/publish-with-retry
        with:
          modrinth-id: ${{ steps.publish_check.outputs.modrinth == 'true' && vars.MODRINTH_PROJECT_ID || '' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: ${{ steps.publish_check.outputs.curseforge == 'true' && vars.CURSEFORGE_PROJECT_ID || '' }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          files: release-artifacts/playermob-fabric-${{ env.MOD_VERSION }}+26.2.jar
          name: PlayerMob ${{ env.RELEASE_TAG }} (Fabric 26.2)
          version: ${{ env.RELEASE_TAG }}+fabric-26.2
          version-type: ${{ steps.release_type.outputs.type }}
          changelog-file: release-notes.md
          loaders: fabric
          game-versions: "26.2"
          java: 25

      - name: Publish NeoForge 26.2 to Modrinth + CurseForge
        id: publish_neoforge_262
        if: steps.publish_check.outputs.modrinth == 'true' || steps.publish_check.outputs.curseforge == 'true'
        uses: ./.github/actions/publish-with-retry
        with:
          modrinth-id: ${{ steps.publish_check.outputs.modrinth == 'true' && vars.MODRINTH_PROJECT_ID || '' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: ${{ steps.publish_check.outputs.curseforge == 'true' && vars.CURSEFORGE_PROJECT_ID || '' }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          files: release-artifacts/playermob-neoforge-${{ env.MOD_VERSION }}+26.2.jar
          name: PlayerMob ${{ env.RELEASE_TAG }} (NeoForge 26.2)
          version: ${{ env.RELEASE_TAG }}+neoforge-26.2
          version-type: ${{ steps.release_type.outputs.type }}
          changelog-file: release-notes.md
          loaders: neoforge
          game-versions: "26.2"
          java: 25

      # After the GitHub Release lands (auto=false), mark the shipped changelog
      # entries released so the next release's "unreleased" set starts clean.
      # GATED ON THE GITHUB-RELEASE STEP, NOT whole-job success(): GitHub is the
      # canonical release, so a flaky Modrinth/CurseForge upload must never again
      # leave the ledger out of sync with a live release (the v0.61.0 failure).
      # Re-run safe: with no unreleased entries mark-released.py is a no-op, so a
      # republish dispatch won't double-commit. This repo has no auto-release
      # cascade today, so every release is real and entries are always marked;
      # the auto gate is kept for parity with DT and forward-compatibility.
      # Commits to main with [skip ci] (no version-bump or build workflows here
      # to trigger anyway).
      - name: Mark changelog entries released
        if: ${{ always() && steps.gh_release.outcome == 'success' && inputs.auto == false }}
        env:
          RELEASE_TAG: ${{ env.RELEASE_TAG }}
        run: |
          set -euo pipefail
          python3 scripts/release-notes/mark-released.py --released-in "$RELEASE_TAG"
          if git diff --quiet -- .github/release-notes/changelog.json; then
            echo "No changelog entries to mark — nothing to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/release-notes/changelog.json
          git commit -m "chore(release-notes): mark $RELEASE_TAG released [skip ci]"
          for attempt in 1 2; do
            if git push origin HEAD:main; then
              echo "Pushed changelog update on attempt $attempt."
              exit 0
            fi
            if [ "$attempt" = "2" ]; then
              echo "::error::Failed to push changelog update after rebase retry."
              exit 1
            fi
            echo "Push rejected — fetching and rebasing before retry."
            git fetch origin main
            git rebase origin/main
          done

      - name: Check Discord webhook prerequisites
        id: discord_check
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -n "$DISCORD_WEBHOOK_URL" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::DISCORD_WEBHOOK_URL secret not set — skipping Discord notification."
          fi

      # Discord is opt-in: fires on MAJOR bumps (X.0.0) or when notify_discord
      # is ticked at dispatch time. always() so partial-failure releases still
      # notify (with ⚠️ markers on platforms that didn't get the upload).
      # RELEASE_TAG guard suppresses notify if the workflow exploded before
      # tag verification.
      - name: Notify Discord
        if: ${{ always() && steps.discord_check.outputs.ready == 'true' && env.RELEASE_TAG != '' && (inputs.notify_discord || steps.release_type.outputs.major_bump == 'true') }}
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RELEASE_TAG: ${{ env.RELEASE_TAG }}
          REPO: ${{ github.repository }}
          PRERELEASE: ${{ steps.release_type.outputs.prerelease }}
          MODRINTH_FABRIC_VERSION: ${{ steps.publish_fabric.outputs.modrinth-version }}
          MODRINTH_FORGE_VERSION: ${{ steps.publish_forge.outputs.modrinth-version }}
          MODRINTH_NEOFORGE_VERSION: ${{ steps.publish_neoforge.outputs.modrinth-version }}
          CURSEFORGE_FABRIC_VERSION: ${{ steps.publish_fabric.outputs.curseforge-version }}
          CURSEFORGE_FORGE_VERSION: ${{ steps.publish_forge.outputs.curseforge-version }}
          CURSEFORGE_NEOFORGE_VERSION: ${{ steps.publish_neoforge.outputs.curseforge-version }}
          GH_TOKEN: ${{ github.token }}
        run: bash scripts/notify-discord.sh
