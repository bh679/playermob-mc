#This file is used by Gradle to provision the daemon JVM. MC 26.2's Loom setup runs
#on the Gradle daemon and requires Java 25 (26.x ships a Java 25 runtime); building
#1.20.1 + 1.21.1 on a Java 25 daemon is fine (their toolchains cap bytecode at 17/21).
#Provisioned automatically via the foojay resolver (settings.gradle.kts).
toolchainVersion=25
